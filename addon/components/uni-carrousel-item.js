import Component from '@ember/component';
import layout from '../templates/components/uni-carrousel-item';

export default Component.extend({
  classNames: ['uni-carrousel__container__item'],
  layout
});
