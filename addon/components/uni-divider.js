import Ember from 'ember';
import layout from '../templates/components/uni-divider';

const { Component } = Ember;

export default Component.extend({
  layout,
  classNames: ['uni-signin__divider']
});
