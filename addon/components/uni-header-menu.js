import Component from '@ember/component';
import layout from '../templates/components/uni-header-menu';

export default Component.extend({
  classNames: ['uni-header-menu'],
  tagName: '',
  layout
});
