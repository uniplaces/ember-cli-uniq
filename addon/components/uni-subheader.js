import Ember from 'ember';
import layout from '../templates/components/uni-subheader';

const { Component } = Ember;

export default Component.extend({
  tagName: '',
  classNames: ['uni-subheader'],
  layout
});
