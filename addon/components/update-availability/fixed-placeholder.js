import Ember from 'ember';
import layout from '../../templates/components/update-availability/fixed-placeholder';

const { Component } = Ember;

export default Component.extend({
  classNames: ['ember-power-select-placeholder'],
  layout
});
