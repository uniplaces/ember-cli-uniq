{{input class="uni-input uni-input--bordered" value=formattedDate click=(action "toggleDatepicker")}}

{{#if showDatepicker}}
  {{uni-datepicker select=selected onSelect=(action "onSelect") center=center onCenter=(action (mut center))}}
{{/if}}

{{yield}}
