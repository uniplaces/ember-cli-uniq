<div class="uni-header__wrapper">
  {{#if logoUrl}}
    <a href="{{logoUrl}}">
      {{svg-jar (if isTransparent "uniplaces_logo_white" "uniplaces_logo")}}
    </a>
  {{else if logoRoute}}
    {{#link-to logoRoute}}
      {{svg-jar (if isTransparent "uniplaces_logo_white" "uniplaces_logo")}}
    {{/link-to}}
  {{else}}
    {{svg-jar (if isTransparent "uniplaces_logo_white" "uniplaces_logo")}}
  {{/if}}

  {{yield}}
</div>
