import calculateOldRent from 'dummy/utils/calculate-old-rent';
import { module, test } from 'qunit';

module('Unit | Utility | calculate-old-rent', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = calculateOldRent();
    assert.ok(result);
  });
});
